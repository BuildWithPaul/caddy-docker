services:
  # ── VitaminChecker ──────────────────────────────────────────
  vitaminchecker:
    build:
      context: ../VitaminChecker
      dockerfile: Dockerfile
    container_name: vitaminchecker
    restart: unless-stopped
    environment:
      - FLASK_ENV=production
      - APPLICATION_ROOT=/vitaminchecker
    volumes:
      - ../VitaminChecker/uploads:/app/uploads

  # ── ChouChouAlerte ──────────────────────────────────────────
  chouchou:
    build:
      context: ../ChouChouAlerte
      dockerfile: Dockerfile
    container_name: chouchou
    restart: unless-stopped
    env_file:
      - ../ChouChouAlerte/.env
    volumes:
      - chouchou-data:/app/data

  # ── Caddy reverse proxy ──────────────────────────────────────
  caddy:
    image: caddy:2
    container_name: caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - vitaminchecker
      - chouchou

volumes:
  caddy_data:
  caddy_config:
  chouchou-data: